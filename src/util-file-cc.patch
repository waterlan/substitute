diff --git a/src/util/file.cc b/src/util/file.cc
index e646bd1..c961e03 100644
--- a/src/util/file.cc
+++ b/src/util/file.cc
@@ -1,4 +1,5 @@
 // Copyright (C) 1994-2026 Ondrej Popp
+// Copyright (C) 2003-2026 Erwin Waterlander
 // 
 // This program is free software: you can redistribute it and/or modify
 // it under the terms of the GNU General Public License as published by
@@ -19,6 +20,7 @@
 #include <string.h>
 #include <sys/stat.h>
 #include <unistd.h>
+#include <errno.h>
 
 #include "std_macro.h"
 #include "structures.h"
@@ -81,6 +83,8 @@ expfun FILE * openW(text filename,
    size_t index;
    char moveName[STRING_SIZE];
    FILE *fo;
+   char *errstr;
+
    if (existsFile(filename) &&
        Backup)
    {
@@ -92,8 +96,13 @@ expfun FILE * openW(text filename,
          index = index + 1;
       }
 
-      fprintf(getLogStream(), "\n- existing file '%s' has been moved to '%s',", filename, moveName);
-      rename(filename, moveName);
+      if (rename(filename, moveName) == -1)
+      {
+	  errstr = strerror(errno);
+	  fprintf(stderr, "****** Substitute : problems renaming %s to %s: %s\n", filename, moveName, errstr);
+      }
+      else
+          fprintf(getLogStream(), "\n- existing file '%s' has been moved to '%s',", filename, moveName);
    }
 
    fo = fopen(filename, "w");
@@ -101,7 +110,9 @@ expfun FILE * openW(text filename,
       fprintf(getLogStream(), "\n- generating '%s'\n", filename);
    else
    {
-      fprintf(getErrorStream(), "\n- couldn't open '%s', using stdout...\n", filename);
+      errstr = strerror(errno);
+      fprintf(getErrorStream(), "\n- couldn't open '%s' for write: %s\n", filename, errstr);
+      fprintf(getErrorStream(), "- using stdout...\n");
       fo = stdout;
    }
 
